// Non-HLSL helper header that should be ignored by the preprocessor.
// Contains C-style declarations that are not valid HLSL.
typedef struct _OpaqueItem {
    int count;
    int flags;
} OpaqueItem;

static const int gOpaqueValue = 7;
